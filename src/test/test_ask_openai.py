"""
File to hold tests related to AskOpenAI
"""

import boto3
import pytest


class TestMessage:

    def test_should_return_a_dictionary():
        assert False
        # dictionary() -> dict:

class TestMessageFactory:

    def test_should_create_a_message():
        assert False
        # create_message(, role: str, content: str) -> dict:

class TestAskOpenAI:

    def test_should_ask():
        assert False
        # ask() -> str:
    
    def test_should__build_messages():
        assert False
        # _build_messages(, question: str) -> list:
